/**
 * Value used to hide the success alerts automatically after that threshold.
 */
export const SUCCESS_ALERT_HIDE_MS = 6000;

/**
 * Value used to hide the error alerts automatically after that threshold.
 */
export const ERROR_ALERT_HIDE_MS = 6000;

/**
 * Value used to control the amount of rows retrieved from pageable components.
 */
export const PAGE_SIZE = 30;

/**
 * Enum used for the alert levels.
 */
export const ALERT_TYPES = {
  info: 'info',
  warn: 'warn',
  error: 'error',
  success: 'success',
};