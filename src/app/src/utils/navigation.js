const login = 'login'
const addChickenCount = 'addChickenCount';

const navigation = Object.freeze({
  root: '/',
  login: `/${login}`,
  addChickenCount: `/${addChickenCount}`
});

export default navigation;